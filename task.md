在目录./qr_fig下面包含5个文件夹，每个文件夹均包含一些二维码的图片，有正常的也有异常的。其中version1、version5和version10三个文件夹包含一些finder pattern部分区域被遮挡的二维码；Rotated文件夹进一步包含经过旋转的遮挡二维码；print文件夹则是把二维码打印出来之后进行拍照的结果，同样包含正常和finder pattern被遮挡的二维码。

请完成以下任务
在现有的代码基础上进行修改。整体思路：先定位，再识别，最后重建。需要将一个可能倾斜、有噪声的QR码图像，转化为一个干净的、轴对齐的标准化图像，供下游解码器使用。具体流程可以如下：
1. 首先进行内容边界检测 + 模块尺寸估计，找到QR码内容区域的包围盒，统计黑像素游程直方图估计模块大小
2. 用主成分分析估计内容方向，决定是否启用旋转候选搜索
3. 对每个QR版本，计算模块大小，搜索最佳网格位置和方向，用固定模块匹配率评分
4. 归一化图像生成：用最佳网格从原图采样每个模块，固定模块用规范值，数据模块用采样值，生成经过缩放的模块图       
5. 修复的遮挡二维码版本号需要支持从version1至version10
6. 尽可能在低时延下把被遮挡的二维码解出来，对于过渡遮挡的场景考虑放弃
7. 如果上面有遗漏或者不合理的操作，请根据你的建议直接修改，需要保证所有二维码都能正确解码。

必须满足以下编程规范：
1. 函数的参数个数最多5个
2. 每个函数的行数控制在100行以内
3. 以最小的代码量实现应有的功能